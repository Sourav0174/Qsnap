import 'dart:ui';

import 'package:flutter/material.dart';

class AppColors {
  static const Color background = Color(0xFFF5F9FF);
  static const Color white = Colors.white;
  static const Color primary = Color(0xFF4DA8DA); // Soft blue
  static const Color darkBlue = Color(0xFF0D3B66); // Text / headings
  static const Color secondary = Color(0xFFEF8354); // Accent (orange-ish)
}
